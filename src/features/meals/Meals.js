import React, { useState } from "react"; import MealForm from "./components/MealForm"; import MealList from "./components/MealList"; import "./Meals.css"; const Meals = () => { const [meals, setMeals] = useState([]); const [editingMeal, setEditingMeal] = useState(null); const handleEditMeal = (meal) => { setEditingMeal(meal); }; return ( <div className="meals-container"> <h2>Meal Tracker</h2> <MealForm onAddMeal={handleAddMeal} editingMeal={editingMeal} /> <MealList meals={meals} onDelete={handleDeleteMeal} onEdit={handleEditMeal} /> </div> ); }; export default Meals;
